import { styled } from "stitches.config";

export default styled("div", {
  display: "flex",
  flexDirection: "column",
  background: "$cardBg",
  color: "$cardText",
  height: "400px",
  width: "300px",
  borderRadius: "3px",
  padding: "$1",
  margin: "$2",
});
